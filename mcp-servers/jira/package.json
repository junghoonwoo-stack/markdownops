{
  "name": "@markdownops/mcp-jira",
  "version": "0.1.0",
  "description": "MarkdownOps MCP server for Jira Cloud — exposes the standard mdops_* tool family.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "markdownops-jira": "./dist/index.js"
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
